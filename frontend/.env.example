BACKEND_API_URL=http://localhost:8000

# Set to "true" once this app is served over HTTPS (directly or via a
# reverse proxy). Until then, leave unset/false — a Secure cookie set over
# plain HTTP is silently dropped by browsers, which breaks login.
COOKIE_SECURE=false
